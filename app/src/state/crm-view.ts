/**
 * Глобальное состояние ленты CRM: упорядоченный массив id сотрудников,
 * отфильтрованных и отсортированных текущим CrmScreen. Используется
 * EmployeeDetailScreen для навигации prev/next в порядке, который видит
 * пользователь в списке.
 *
 * При прямом открытии `/crm/:id` (без захода на /crm) signal пуст, и
 * детальная страница использует репозиторий целиком в дефолтном порядке.
 */
import { signal } from '@preact/signals';

export const crmViewSignal = signal<string[]>([]);
